These are just dummies!
In a production environment, add the secrets directory to your gitignore. 
Ensure these secret files (.txt files) have restricted permissions on your host machine so that only the user running Docker can read them. For example, chmod 400 secrets/*.txt.